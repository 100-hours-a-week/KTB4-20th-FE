# PlanIt 프론트엔드 디자인 가이드

## 1. 문서 목적

이 문서는 PlanIt의 화면을 설계하고 구현할 때 적용하는 공통 기준이다. 새로운 화면이나 컴포넌트를 만들기 전에 이 문서와 `src/styles/variables.css`를 확인하고, 디자인 시안으로 규칙이 변경되면 두 파일을 함께 갱신한다.

문서에 없는 제품 정책이나 인터랙션을 임의로 확정하지 않는다. 디자인 시안, 기획 문서와 API 명세가 충돌하면 관련 담당자와 기준을 먼저 합의한다.

## 2. 서비스와 디자인 방향

PlanIt은 친구나 동료가 함께 떠나는 국내 여행을 계획하고 경험하는 그룹 여행 서비스다. 구성원별 취향과 희망 장소를 모아 AI가 그룹에 맞는 여행 일정과 이동 동선을 제안하며, 지역 오픈 채팅과 커뮤니티에서 여행지 정보를 얻고 여행 중에는 포토 미션을 수행할 수 있다.

따라서 화면은 다음 가치를 우선한다.

- **함께 이해하기:** 여행방의 참여자, 설문 진행률, 일정 상태와 방장 권한을 누구나 빠르게 파악할 수 있어야 한다.
- **결정 부담 줄이기:** 날짜, 지역, 취향, 희망 장소처럼 복잡한 입력은 한 단계에 하나의 결정에 집중하도록 구성한다.
- **AI 과정 설명하기:** 일정과 포토 미션의 생성·판정 상태를 숨기지 않고, 대기·성공·실패와 다음 행동을 명확히 안내한다.
- **여행 흐름 이어가기:** 여행 전 계획, 여행 중 미션, 여행 후 커뮤니티 탐색이 하나의 서비스처럼 일관되게 느껴져야 한다.
- **정보를 먼저 보여주기:** 장식보다 장소, 날짜, 참여자, 방문 순서와 현재 상태 등 사용자의 판단에 필요한 정보를 우선한다.

## 3. 기본 원칙

- 모바일 화면을 기본으로 설계하고 넓은 화면으로 확장한다.
- 같은 의미에는 같은 색상, 간격, 타이포그래피와 컴포넌트를 사용한다.
- 색상만으로 상태를 구분하지 않고 텍스트, 아이콘 또는 패턴을 함께 제공한다.
- 정상 화면과 함께 로딩, 빈 상태, 오류, 비활성, 권한 제한 상태를 설계한다.
- 사용자가 되돌릴 수 없는 행동에는 결과를 미리 알리고 확인 단계를 제공한다.
- 서버 검증이나 AI 처리가 끝나기 전에 성공 화면을 낙관적으로 확정하지 않는다.
- 날짜와 마감 시각은 서비스 정책의 기준 시간대를 따르고, 화면에는 이해하기 쉬운 형식으로 표시한다.

## 4. 디자인 토큰

색상과 크기를 컴포넌트에 직접 반복해서 작성하지 않고 `src/styles/variables.css`의 CSS 변수를 사용한다. 토큰을 추가할 때는 기존 값으로 표현할 수 없는지 먼저 확인하고, 의미가 드러나는 이름을 사용한다.

### 색상

| 용도 | CSS 변수 | 값 | 사용 예시 |
| --- | --- | --- | --- |
| 주요 동작 | `--color-primary` | `#2563eb` | 다음, 참여, 생성, 확인 |
| 주요 동작 강조 | `--color-primary-dark` | `#1d4ed8` | 호버, 눌림 |
| 기본 글자 | `--color-text` | `#1f2024` | 제목, 본문, 핵심 정보 |
| 보조 글자 | `--color-text-muted` | `#6b7280` | 설명, 메타 정보, 보조 라벨 |
| 페이지 배경 | `--color-background` | `#ffffff` | 기본 화면 배경 |
| 보조 배경 | `--color-surface` | `#f5f6f8` | 카드 구분, 입력 영역, 빈 상태 |
| 테두리 | `--color-border` | `#e2e4e8` | 입력 필드, 카드, 구분선 |
| 오류·위험 | `--color-danger` | `#dc2626` | 유효성 오류, 실패, 위험 동작 |
| 성공 | `--color-success` | `#16a34a` | 제출 완료, 생성 성공, 미션 완료 |

`--color-primary`는 사용자가 지금 수행해야 할 핵심 동작에 제한적으로 사용한다. 여행 상태나 카테고리를 구분하기 위해 임의의 색상을 추가하지 않는다.

### 간격

4px 단위를 기준으로 다음 토큰을 사용한다.

| 토큰 | 값 | 권장 용도 |
| --- | --- | --- |
| `--spacing-1` | `4px` | 아이콘과 짧은 라벨 사이 |
| `--spacing-2` | `8px` | 밀접한 요소 사이 |
| `--spacing-3` | `12px` | 입력 필드 내부, 작은 묶음 |
| `--spacing-4` | `16px` | 모바일 기본 여백, 카드 내부 |
| `--spacing-5` | `24px` | 섹션 내부, 항목 그룹 사이 |
| `--spacing-6` | `32px` | 주요 섹션 사이 |
| `--spacing-7` | `48px` | 페이지 상하 여백 |
| `--spacing-8` | `64px` | 넓은 화면의 큰 구획 사이 |

### 타이포그래피

- 기본 글꼴은 `--font-family-base`를 사용한다.
- 보조 문구는 `--font-size-sm`, 본문은 `--font-size-base`를 사용한다.
- 카드나 섹션 제목은 `--font-size-lg` 또는 `--font-size-xl`을 사용한다.
- 페이지의 대표 제목은 `--font-size-2xl`을 사용한다.
- 제목은 시각적 크기가 아니라 문서 구조에 맞춰 `h1`부터 순서대로 사용한다.
- 날짜, 인원, 거리와 진행률처럼 비교해야 하는 값은 같은 화면에서 동일한 표기 형식을 유지한다.
- 버튼과 라벨은 짧고 행동 중심으로 작성한다. 모호한 “확인”보다 “여행방 참여”, “일정 생성”처럼 결과가 드러나는 문구를 우선한다.

### 모서리와 그림자

- 작은 입력 요소와 버튼은 `--radius-sm`, 카드와 큰 영역은 `--radius-md`를 사용한다.
- 그림자는 `--shadow-sm`을 기본으로 하며, 클릭 가능한 카드나 떠 있는 영역의 구분이 필요할 때만 사용한다.
- 모서리와 그림자만으로 요소의 클릭 가능 여부를 표현하지 않는다.

## 5. 레이아웃과 반응형

- 페이지 콘텐츠는 `--max-width-content` 안에서 중앙 정렬한다.
- 모바일 좌우 여백은 최소 `--spacing-3`, 일반 화면은 `--spacing-4` 이상을 사용한다.
- 기본 반응형 기준은 768px이며, 콘텐츠가 실제로 깨지는 지점에서만 기준을 추가한다.
- 한 화면에는 하나의 대표 `h1`을 두고, 핵심 행동은 가능한 한 하나만 강조한다.
- 입력 흐름은 짧은 단계로 나누고 현재 단계와 남은 과정을 알 수 있게 한다.
- 긴 일정, 채팅, 커뮤니티 목록은 항목 간 위계와 날짜 구분이 명확해야 한다.
- 여행 동선은 방문 순서가 가장 먼저 읽혀야 하며, 장소 상세 정보가 순서를 방해하지 않게 한다.
- 모바일에서 표나 다열 레이아웃은 카드 또는 단일 열로 전환한다. 불가피한 데이터 표를 제외하고 페이지 전체의 가로 스크롤을 허용하지 않는다.
- 하단 고정 버튼은 본문을 가리지 않도록 안전 영역과 충분한 하단 여백을 확보한다.

## 6. 공통 컴포넌트

### 버튼

- 주요 버튼은 `--color-primary`, 호버와 눌림은 `--color-primary-dark`를 사용한다.
- 터치 영역은 최소 44×44px을 확보한다.
- 한 영역에서 주요 버튼을 여러 개 경쟁시키지 않는다. 보조 행동은 외곽선 또는 텍스트 버튼으로 구분한다.
- 처리 중에는 중복 요청을 막고 로딩 상태를 버튼 안이나 가까운 곳에 표시한다.
- 비활성 상태는 `disabled` 속성을 사용하고, 사용자가 이유를 알기 어려운 경우 짧은 안내를 함께 제공한다.
- 아이콘만 있는 버튼에는 `aria-label` 등 접근 가능한 이름을 제공한다.

### 입력 필드와 선택 요소

- 모든 입력 필드에는 연결된 `label`을 제공하며 placeholder만으로 목적을 설명하지 않는다.
- 필수 여부, 허용 범위와 글자 수 제한은 입력 전에 확인할 수 있게 한다.
- 오류는 문제가 발생한 필드 가까이에 `--color-danger`와 구체적인 해결 방법으로 표시한다.
- 날짜, 인원, 평점처럼 범위가 정해진 값은 자유 입력보다 달력, 스테퍼, 척도 등 제약이 드러나는 컨트롤을 우선한다.
- 선택 결과가 다음 단계에 영향을 주면 변경 시 무엇이 초기화되는지 알려준다.

### 카드와 목록

- 카드는 여행방, 일정, 커뮤니티 게시물처럼 하나의 정보 단위를 묶을 때 사용한다.
- 같은 종류의 카드에는 제목, 상태, 메타 정보와 행동의 위치를 일관되게 유지한다.
- 여행방 카드는 이름뿐 아니라 지역, 기간과 진행 상태를 함께 제공한다.
- 상태 배지는 짧은 텍스트를 포함하고, 색상만으로 의미를 전달하지 않는다.
- 목록이 비어 있으면 단순히 빈 영역을 보여주지 않고 이유와 가능한 다음 행동을 안내한다.

### 내비게이션과 피드백

- 현재 위치와 뒤로 가기의 결과를 예측할 수 있어야 한다.
- 다단계 생성 흐름을 이탈할 때 입력 손실 가능성이 있으면 확인한다.
- 토스트는 작업 결과처럼 짧게 확인할 정보에 사용하고, 사용자의 결정이나 재시도가 필요한 오류는 화면 안에 유지한다.
- 모달은 즉시 판단이 필요한 짧은 작업에만 사용하며, 복잡한 입력은 별도 화면으로 제공한다.

## 7. 서비스 기능별 표현 원칙

### 여행방과 취향 조사

- 방장과 현재 사용자를 텍스트로 명확히 구분한다.
- 설문 진행률은 완료 인원과 전체 인원을 함께 표시한다.
- 취향 척도는 모든 문항에서 같은 방향과 라벨을 사용하고 현재 선택값을 분명히 보여준다.
- 희망 장소 검색 결과와 실제 선택된 장소를 시각적으로 구분한다.

### AI 일정과 여행 동선

- 생성 전, 생성 중, 후보 확인, 확정, 실패 상태를 서로 구분한다.
- 생성 중에는 예상 결과를 임의로 보여주지 않고 현재 처리 중임을 안내한다.
- 실패 화면에는 재시도 가능 여부와 기존 일정 유지 여부 등 다음 행동을 제공한다.
- 일정에는 Day, 시간대, 방문 순서와 장소를 일관된 위계로 표시한다.
- AI 추천 결과와 사용자가 직접 변경한 결과를 혼동하지 않도록 필요한 경우 출처나 변경 상태를 표시한다.

### 지역 오픈 채팅과 커뮤니티

- 오픈 채팅은 여행방 내부 채팅이 아니라 모든 사용자가 참여하는 지역별 공개 채팅임을 진입 화면에서 알린다.
- 메시지 작성자, 시간, 전송 상태와 제재 안내가 서로 혼동되지 않아야 한다.
- 커뮤니티 카드는 지역, 여행 기간 또는 일정 요약 등 탐색에 필요한 정보를 우선한다.
- 필터 적용 상태와 결과 없음 상태를 명확히 구분한다.

### 포토 미션

- 수행 가능, 판정 중, 성공, 유사, 실패, 직접 완료 상태를 텍스트와 시각 요소로 함께 구분한다.
- 사진 제출 전에 허용 형식과 시점을 안내하고, 업로드·판정 중 중복 제출을 막는다.
- AI 판정 결과는 점수만 보여주지 않고 인식 결과와 사용자가 할 수 있는 다음 행동을 설명한다.
- 그룹 미션에서는 나의 참여 상태와 그룹 전체 완료 상태를 구분한다.

## 8. 로딩, 빈 상태와 오류

- 로딩 중에는 레이아웃이 크게 흔들리지 않도록 콘텐츠 형태에 맞는 자리 표시자를 사용한다.
- 초기 로딩, 추가 목록 로딩과 작업 처리 중 상태를 구분한다.
- 빈 상태는 “아직 여행이 없음”, “필터 결과 없음”, “메시지 없음”처럼 원인을 구체적으로 설명한다.
- 오류 메시지는 내부 코드 대신 사용자 관점의 원인과 해결 방법을 제공한다.
- 재시도할 수 있는 오류에는 명확한 재시도 버튼을 제공한다.
- 인증 만료, 권한 부족, 잘못된 초대 링크와 삭제된 여행방처럼 행동이 다른 오류는 하나의 일반 오류로 합치지 않는다.
- 기존 데이터를 유지할 수 있는 실패라면 데이터가 보존됐음을 함께 알린다.

## 9. 접근성

- 키보드만으로 모든 링크, 버튼, 입력 필드와 모달을 사용할 수 있어야 한다.
- 포커스 표시를 제거하지 않으며 배경과 충분히 대비되게 한다.
- 행동에는 `button`, 화면 이동에는 `a` 또는 React Router의 `Link`를 사용한다.
- 이미지에는 용도에 맞는 `alt`를 제공하고 장식용 이미지는 빈 `alt`를 사용한다.
- 아이콘과 상태 색상에는 텍스트 대안을 제공한다.
- 모달이 열리면 포커스를 내부로 이동하고 닫은 뒤에는 실행 요소로 돌려보낸다.
- 동적으로 변경되는 생성 결과, 오류와 업로드 상태는 보조 기술에도 전달되도록 적절한 라이브 영역을 사용한다.
- 본문과 주요 UI는 확대하거나 좁은 화면에서 사용해도 정보나 기능이 사라지지 않아야 한다.

## 10. 구현 규칙

- 전역 토큰과 초기화 스타일은 `src/styles`에서 관리한다.
- 페이지와 컴포넌트 스타일은 CSS Modules로 작성한다.
- 컴포넌트와 CSS Module 파일은 PascalCase로 이름을 짓는다.
- 공통 UI는 `src/components`, 페이지 전용 UI는 해당 `src/pages` 하위에 둔다.
- 색상, 간격과 글꼴 크기의 임의 값을 반복하지 않고 토큰을 사용한다.
- 새 디자인 규칙을 도입하면 이 문서와 `src/styles/variables.css`를 함께 갱신한다.
- 브라우저 기본 포커스나 시맨틱을 제거할 때는 동등하거나 더 나은 접근성을 함께 구현한다.
- 실제 API 응답 전에는 임의의 상태값이나 인증 방식을 디자인 규칙으로 고정하지 않는다.

## 11. 화면 검토 체크리스트

- [ ] 사용자가 이 화면의 목적과 현재 상태를 바로 이해할 수 있는가?
- [ ] 가장 중요한 정보와 대표 행동이 먼저 보이는가?
- [ ] 로딩, 빈 상태, 오류, 비활성과 권한 제한 상태가 준비되어 있는가?
- [ ] 모바일 화면과 긴 텍스트, 최대 인원·항목 조건에서도 레이아웃이 유지되는가?
- [ ] 색상 없이도 상태와 행동을 구분할 수 있는가?
- [ ] 키보드 탐색, 포커스 표시와 접근 가능한 이름이 제공되는가?
- [ ] 기존 토큰과 공통 컴포넌트를 재사용했는가?
- [ ] AI 또는 서버 처리 결과와 사용자가 할 수 있는 다음 행동이 명확한가?
