# PlanIt 디자인 규칙

이 문서는 프론트엔드 화면을 만들거나 수정할 때 적용하는 기본 디자인 기준이다. 확정된 디자인 시안이 생기면 이 문서와 `src/styles/variables.css`를 함께 수정한다.

## 1. 기본 원칙

- 화면 전체에서 같은 색상, 간격, 모서리 값과 글자 크기를 사용한다.
- 장식보다 일정과 작업 정보를 빠르게 파악하는 데 집중한다.
- 모바일 화면을 기본으로 작성하고 넓은 화면으로 확장한다.
- 로딩, 빈 상태, 오류, 비활성 상태를 정상 화면과 함께 설계한다.
- 색상만으로 상태를 구분하지 않고 텍스트나 아이콘을 함께 사용한다.

## 2. 디자인 토큰

임의의 값을 반복해서 작성하지 않고 `src/styles/variables.css`의 CSS 변수를 사용한다.

### 색상

| 용도 | CSS 변수 | 값 |
| --- | --- | --- |
| 주요 동작 | `--color-primary` | `#2563eb` |
| 주요 동작 호버 | `--color-primary-dark` | `#1d4ed8` |
| 기본 글자 | `--color-text` | `#1f2024` |
| 보조 글자 | `--color-text-muted` | `#6b7280` |
| 페이지 배경 | `--color-background` | `#ffffff` |
| 보조 배경 | `--color-surface` | `#f5f6f8` |
| 테두리 | `--color-border` | `#e2e4e8` |
| 오류 | `--color-danger` | `#dc2626` |
| 성공 | `--color-success` | `#16a34a` |

### 간격

4px 단위를 기준으로 `--spacing-1`부터 `--spacing-8`까지 사용한다. 새로운 간격이 필요하면 기존 토큰으로 표현할 수 없는지 먼저 확인한다.

### 글꼴

- 기본 글꼴: `--font-family-base`
- 본문 기본 크기: `--font-size-base`
- 보조 문구: `--font-size-sm`
- 페이지 제목: `--font-size-2xl`
- 제목은 의미에 맞는 `h1`부터 `h6`까지 순서대로 사용한다.

## 3. 레이아웃

- 콘텐츠 최대 너비는 `--max-width-content`를 사용한다.
- 모바일 좌우 여백은 최소 `--spacing-3`, 일반 화면은 `--spacing-4` 이상을 사용한다.
- 기본 반응형 기준은 768px이다. 화면 내용에 따라 필요한 경우에만 기준을 추가한다.
- 가로 스크롤이 필요한 표를 제외하고 페이지 전체에 가로 스크롤이 생기지 않게 한다.

## 4. 컴포넌트

### 버튼

- 주요 버튼은 `--color-primary`, 호버는 `--color-primary-dark`를 사용한다.
- 클릭 영역의 높이는 최소 44px을 권장한다.
- 아이콘만 있는 버튼에는 접근 가능한 이름을 제공한다.
- 비활성 상태는 클릭할 수 없도록 `disabled` 속성을 사용한다.

### 입력 필드

- 모든 입력 필드에는 연결된 `label`을 제공한다.
- 오류 문구는 입력 필드 가까이에 표시하고 `--color-danger`를 사용한다.
- placeholder만으로 입력 목적을 설명하지 않는다.

### 카드와 영역

- 배경은 `--color-background` 또는 `--color-surface`를 사용한다.
- 테두리는 `--color-border`, 모서리는 `--radius-sm` 또는 `--radius-md`를 사용한다.
- 같은 역할의 카드에는 같은 내부 여백을 적용한다.

## 5. 상호작용과 접근성

- 키보드로 모든 링크, 버튼과 입력 필드에 접근할 수 있어야 한다.
- 포커스 표시를 제거하지 않는다.
- 버튼에는 `button`, 이동에는 `a` 또는 React Router의 `Link`를 사용한다.
- 이미지에는 용도에 맞는 `alt`를 작성하고 장식용 이미지는 빈 `alt`를 사용한다.
- 로딩 중에는 중복 제출을 막고 진행 상태를 사용자에게 알린다.

## 6. 구현 규칙

- 전역 토큰과 초기화 스타일은 `src/styles`에서 관리한다.
- 페이지 및 컴포넌트 스타일은 CSS Modules로 작성한다.
- 컴포넌트 파일과 CSS Module 파일의 이름은 PascalCase로 작성한다.
- 새 색상, 간격, 폰트 크기를 추가할 때는 먼저 디자인 토큰에 정의한다.
- 확정된 디자인 시안이 이 문서와 다르면 시안을 반영하면서 이 문서도 갱신한다.
