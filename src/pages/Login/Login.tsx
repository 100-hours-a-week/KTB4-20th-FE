import styles from '../PagePlaceholder.module.css';

export default function Login() {
  return (
    <main className={styles.container}>
      <h1 className={styles.title}>로그인</h1>
      <p className={styles.description}>카카오 로그인 화면이 구현될 예정입니다.</p>
    </main>
  );
}
